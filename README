# edenred
A small app to check your Edenred meal card balance

## Usage

Just set the `USER` and `PASS` env variables a run the script

To run it with docker...

```
docker build . -t edenred
docker run -e USER=111 -e PASS=222 edenred
```

